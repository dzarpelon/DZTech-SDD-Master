name: CI

on:
  push:
  pull_request:

jobs:
  placeholder-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder validation
        run: echo "Placeholder validation job"
