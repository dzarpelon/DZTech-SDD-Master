# Specs

This directory stores specification-stage artifacts for the Spec-Driven Development flow.

Relevant workflow states:

- `spec_draft`
- `validated_spec`

Spec approval is a human validation gate before project creation can proceed.
