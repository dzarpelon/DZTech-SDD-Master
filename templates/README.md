# Templates

This directory stores reusable templates and scaffolds for repositories generated or governed by DZTech SDD Master.

Template buildout is intentionally minimal until a source-of-truth file explicitly defines concrete template artifacts.
